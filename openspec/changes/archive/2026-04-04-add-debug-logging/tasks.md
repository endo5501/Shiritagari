## 1. env_logger 初期化

- [x] 1.1 `lib.rs` の `run()` 関数冒頭で `env_logger::init()` を呼び出す
- [x] 1.2 `RUST_LOG=debug npm run tauri dev` でログが出力されることを確認する

## 2. poller.rs のログ追加

- [x] 2.1 AFK 判定結果のログ追加（AFK 時: info、アクティブ時: debug）
- [x] 2.2 イベント取得件数のログ追加（全取得数、フィルタ後の新規イベント数、カーソル位置）

## 3. engine.rs のログ追加

- [x] 3.1 プライバシーフィルタ結果のログ追加（フィルタ前後の件数、全除外時は info）
- [x] 3.2 パターンマッチング結果のログ追加（アプリ名、実効信頼度、アクション）
- [x] 3.3 LLM 推論結果のログ追加（confidence、should_ask、アクション）

## 4. lib.rs メインループのログ追加

- [x] 4.1 サイクル開始ログの追加（debug）
- [x] 4.2 LLM プロバイダー生成失敗のログ追加（warn、エラー内容を含む）
- [x] 4.3 質問送信ログの追加（info）
- [x] 4.4 サイクル完了・acknowledge 完了ログの追加（debug）

## 5. 動作確認

- [x] 5.1 `RUST_LOG=debug` で全判定ポイントのログが出力されることを確認する
- [x] 5.2 `RUST_LOG=info` で重要な判定結果のみが出力されることを確認する
- [x] 5.3 `RUST_LOG` 未設定で既存の動作に影響がないことを確認する
